from django.apps import AppConfig

class UseraccountsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'useraccounts'
    verbose_name = 'Usuário'
    label = "gc_useraccounts"
